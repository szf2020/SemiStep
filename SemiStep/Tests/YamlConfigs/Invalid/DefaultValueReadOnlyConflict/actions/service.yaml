# Invalid: action references read-only column with a default_value
10:
  ui_name: "Wait"
  deploy_duration: longlasting
  columns:
    - key: step_duration
      default_value: "10"
      property_type_id: time
    - key: readonly_col
      default_value: "should_not_be_allowed"
      property_type_id: string
    - key: comment
      property_type_id: string

20:
  ui_name: "For"
  deploy_duration: immediate
  columns:
    - key: task
      default_value: "1"
      property_type_id: float
    - key: comment
      property_type_id: string

30:
  ui_name: "EndFor"
  deploy_duration: immediate
  columns:
    - key: comment
      property_type_id: string

40:
  ui_name: "Pause"
  deploy_duration: immediate
  columns:
    - key: comment
      property_type_id: string
