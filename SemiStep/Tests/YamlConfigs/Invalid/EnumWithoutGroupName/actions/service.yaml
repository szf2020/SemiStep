# Invalid: action column uses enum property but omits group_name
10:
  ui_name: "Wait"
  deploy_duration: longlasting
  columns:
    - key: step_duration
      default_value: "10"
      property_type_id: time
    - key: action
      property_type_id: enum
    - key: comment
      property_type_id: string

20:
  ui_name: "For"
  deploy_duration: immediate
  columns:
    - key: task
      default_value: "1"
      property_type_id: float
    - key: comment
      property_type_id: string

30:
  ui_name: "EndFor"
  deploy_duration: immediate
  columns:
    - key: comment
      property_type_id: string

40:
  ui_name: "Pause"
  deploy_duration: immediate
  columns:
    - key: comment
      property_type_id: string
